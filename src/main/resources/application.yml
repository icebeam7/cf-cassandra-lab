spring:
  cassandra:
    contact-points: localhost
    port: 9042
    keyspace-name: products_db
    local-datacenter: datacenter1
    schema-action: create-if-not-exists

server:
  port: 8080

logging:
  level:
    com.demo: DEBUG
    org.springframework.data.cassandra: DEBUG
